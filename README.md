# Life Lessons API Server

This is the Express backend for the Life Lessons platform. It provides APIs to manage lessons, users, reports, and administrative tasks.

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```

2. Configure environment variables in `.env`.

3. Start the server:
   ```bash
   npm start
   ```
